Задача 4.116 - Вычисление функции y(x)

Предположим, функция  определена как . Пример реализации:


def y(x):
    return x**2 + 2

# Пример использования
print(y(3))  # 11

Задача 4.117 - Вычисление функции z(a)

Предположим, функция  определена как . Пример реализации:


import math

def z(a):
    return math.sin(a)

# Пример использования
print(z(math.pi / 2))  # 1.0

Задача 4.118 - Вычисление f(x)

Предположим, функция  определена как . Пример реализации:


def f(x):
    return 2 * x + 3

# Пример использования
print(f(5))  # 13

Задача 4.119 - Вычисление f(y)

Аналогично предыдущей задаче, просто измените аргумент:


y = 4.0
print(f(y))  # 11.0

Задача 4.120 - Определение значения y при заданном x

Сначала необходимо определить функцию:


def piecewise_function(x):
    if x < -1:
        return -1
    elif -1 <= x < 1:
        return x
    else:
        return 1

# Пример использования
print(piecewise_function(0.5))  # 0.5

Задача 4.121 - Определение области для точки

def determine_region(x, y):
    if x > 0 and y > 0:
        return "I"
    elif x < 0 and y > 0:
        return "II"
    elif x < 0 and y < 0:
        return "III"
    else:
        return "Точка на границе"

# Пример использования
print(determine_region(1, 1))  # I

Задача 4.122 - Определение области для точки

Для другого условия, изменим допустимые границы:


def determine_region_v2(x, y):
    if 0 < x <= 2.5 and 0 < y <= 5:
        return "I"
    elif x <= 0 and 0 < y <= 5:
        return "II"
    elif x <= 2.5 and y <= 0:
        return "III"
    else:
        return "Точка на границе"

# Пример использования
print(determine_region_v2(1, 3))  # I

Задача 4.123 - Определение результата футбольной игры

def game_result(points):
    if points == 3:
        return "Выигрыш"
    elif points == 0:
        return "Проигрыш"
    else:
        return "Ничья"

# Пример использования
print(game_result(2))  # Ничья

Задача 4.124 - Сравнение возрастов

def compare_ages(age_mitya, age_vasya):
    if age_mitya > age_vasya:
        return "Митя старше Васи"
    elif age_mitya < age_vasya:
        return "Вася старше Мити"
    else:
        return "Митя и Вася одного возраста"

# Пример использования
print(compare_ages(25, 30))  # Вася старше Мити

Задача 4.125 - Определение весовой категории боксёра

def weight_category(weight):
    if weight < 60:
        return "Легкий вес"
    elif weight < 64:
        return "Первый полусредний вес"
    elif weight < 69:
        return "Полусредний вес"
    else:
        return "Не входит в категории"

# Пример использования
print(weight_category(63))  # Первый полусредний вес

Задача 4.126 - Делимость чисел

def divisibility(a, b):
    if a % b == 0:
        return f"{a} делится на {b}"
    elif b % a == 0:
        return f"{b} делится на {a}"
    else:
        return "Ни одно из чисел не является делителем другого"

# Пример использования
print(divisibility(4, 2))  # 4 делится на 2

Задача 4.127 - Определение наибольшего, наименьшего и среднего

def find_numbers(a, b, c):
    largest = max(a, b, c)
    smallest = min(a, b, c)
    middle = a + b + c - largest - smallest
    return largest, smallest, middle

# Пример использования
print(find_numbers(5, 3, 8))  # (8, 3, 5)

Задача 4.128 - Максимальное и минимальное значения из трех чисел

def max_min(a, b, c):
    return max(a, b, c), min(a, b, c)

# Пример использования
print(max_min(5.5, 3.2, 4.7))  # (5.5, 3.2)

Задача 4.129 - Сумма двух наибольших чисел

def sum_of_two_largest(a, b, c):
    return sum(sorted([a, b, c])[-2:])  # Сумма двух наибольших

# Пример использования
print(sum_of_two_largest(1, 2, 3))  # 5

Задача 4.130 - Произведение двух наименьших чисел

def product_of_two_smallest(a, b, c):
    return sorted([a, b, c])[:2][0] * sorted([a, b, c])[:2][1]  # Произведение двух наименьших

# Пример использования
print(product_of_two_smallest(1, 2, 3))  # 2

Задача 4.131 - Ср. арифмет. ср. для двух троек чисел

def avg_of_medians(trip1, trip2):
    median1 = sorted(trip1)[1]
    median2 = sorted(trip2)[1]
    return (median1 + median2) / 2

# Пример использования
print(avg_of_medians([3, 1, 2], [6, 4, 5]))  # 3.0

Задача 4.132 - Номер четверти координатной плоскости

def quadrant(x, y):
    if x > 0 and y > 0:
        return 1
    elif x < 0 and y > 0:
        return 2
    elif x < 0 and y < 0:
        return 3
    elif x > 0 and y < 0:
        return 4

# Пример использования
print(quadrant(3, -1))  # 4

Задача 4.133 - Название дня недели

def day_of_week(n):
    days = ["Понедельник", "Вторник", "Среда", "Четверг", "Пятница", "Суббота", "Воскресенье"]
    return days[n - 1]

# Пример использования
print(day_of_week(3))  # Среда

Задача 4.134 - Название месяца

def month_name(n):
    months = ["Январь", "Февраль", "Март", "Апрель", "Май", "Июнь", "Июль", "Август", "Сентябрь", "Октябрь", "Ноябрь", "Декабрь"]
    return months[n - 1]

# Пример использования
print(month_name(5))  # Май

Задача 4.135 - Время года

def season(month):
    if month in [12, 1, 2]:
        return "Зима"
    elif month in [3, 4, 5]:
        return "Весна"
    elif month in [6, 7, 8]:
        return "Лето"
    elif month in [9, 10, 11]:
        return "Осень"

# Пример использования
print(season(5))  # Весна

Задача 4.136 - Количество дней в месяце

def days_in_month(month, leap_year=False):
    if month in [1, 3, 5, 7, 8, 10, 12]:
        return 31
    elif month in [4, 6, 9, 11]:
        return 30
    elif month == 2:
        return 29 if leap_year else 28
    else:
        return "Некорректный месяц"

# Пример использования
print(days_in_month(2, leap_year=True))  # 29

Задача 4.137 - Дней в месяце с трехветвевым условным оператором

def days_in_month_simple(month):
    if month in [1, 3, 5, 7, 8, 10, 12]:
        return 31
    elif month in [4, 6, 9, 11]:
        return 30
    elif month == 2:
        return 28  # Принять, что год не високосный

# Пример использования
print(days_in_month_simple(2))  # 28

Задача 4.138 - Название масти игральных карт

def suit_name(m):
    suits = {1: "Пики", 2: "Трефы", 3: "Бубны", 4: "Червы"}
    return suits.get(m, "Некорректная масть")

# Пример использования
print(suit_name(2))  # Трефы

Задача 4.139 - Достоинство игральных карт

def card_value(k):
    if 6 <= k <= 10:
        return str(k)  # Просто число
    elif k == 11:
        return "Валет"
    elif k == 12:
        return "Дама"
    elif k == 13:
        return "Король"
    elif k == 14:
        return "Туз"
    else:
        return "Некорректная карта"

# Пример использования
print(card_value(11))  # Валет

Задача 4.140 - Полное название карты

def full_card_name(m, k):
    return f"{card_value(k)} {suit_name(m)}"

# Пример использования
print(full_card_name(1, 12))  # Дама Пик
